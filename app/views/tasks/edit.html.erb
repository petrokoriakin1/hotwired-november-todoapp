<div>
  <h1 class="font-bold text-2xl mb-3">Editing Task</h1>

  <%= turbo_frame_tag @task do %>
    <%= render "form", task: @task %>
    <%= link_to "Never Mind", list_path(@task.list), class: "btn mb-3 bg-gray-100" %>
  <% end %>
</div>
