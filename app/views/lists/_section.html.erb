<% unless section.default? %>
  <h2 class="text-2xl font-semibold text-gray-700 mb-4"><%= section.title %></h2>
<% end %>

<%= render section.tasks %>

<div class="mt-4">
  <%= render "tasks/form", task: Task.new(section: section) %>
</div>
